name: Test on push
description: Test on push

on:
  push:
    branches:
      - master
      - main
    paths:
      - src/**
      - setup.py
      - pyproject.toml
  pull_request:

jobs:
  tests:
    uses: ./.github/workflows/test-matrix.yaml
