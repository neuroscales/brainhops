[project]
name = "wip-transforms"
version = "0.1.0"
description = "Scalable spatial transforms"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
